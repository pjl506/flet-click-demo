[project]
name = "flet-click-demo"
version = "2.0.0"
description = "Flet 点击计数换色 + WebSocket 订阅/发布 Demo"
dependencies = ["websocket-client>=1.6.0"]

[tool.flet]
org = "com.example"
product = "FletDemo"

[tool.flet.app]
path = "app"
module = "main"
